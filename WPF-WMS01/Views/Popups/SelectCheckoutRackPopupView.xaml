<Window x:Class="WPF_WMS01.Views.Popups.SelectCheckoutRackPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_WMS01.Views.Popups"
        xmlns:viewmodels="clr-namespace:WPF_WMS01.ViewModels.Popups"
        mc:Ignorable="d"
        Title="출고할 랙 선택" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewmodels:SelectCheckoutRackPopupViewModel, IsDesignTimeCreatable=True}"
        DataContextChanged="Window_DataContextChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" ItemsSource="{Binding AvailableRacks}" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DisplayText}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="확인" Command="{Binding ConfirmCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="취소" Command="{Binding CancelCommand}" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>