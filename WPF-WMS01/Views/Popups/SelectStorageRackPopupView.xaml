<Window x:Class="WPF_WMS01.Views.Popups.SelectStorageRackPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_WMS01.Views.Popups"
        xmlns:viewmodels="clr-namespace:WPF_WMS01.ViewModels.Popups"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:SelectStorageRackPopupViewModel}"
        Title="완제품 보관 랙 선택" Height="450" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="이동할 랙을 선택하세요 (ImageIndex 3)" Margin="0,0,0,10" FontSize="14" FontWeight="Bold"/>

        <ListView Grid.Row="1" ItemsSource="{Binding AvailableRacks}" SelectedItem="{Binding SelectedRack}" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn Header="이름" DisplayMemberBinding="{Binding Title}" Width="150"/>
                    <GridViewColumn Header="RackType" DisplayMemberBinding="{Binding RackType}" Width="80"/>
                    <GridViewColumn Header="BulletType" DisplayMemberBinding="{Binding BulletType}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="확인" Command="{Binding SelectCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="취소" Command="{Binding CancelCommand}" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>